# NOTES

## 2026-06-27

- Repo is on `main`, ahead of `origin/main` by one commit: `28529d9 try to improve up-axis and thumbnail gen`.
- Original app scanned `submodules/glTF-Sample-Models/2.0/**/*.glb` and hardcoded `three`, `blender`, and `ov` conversion paths in `src/dynamicFiles.ts`.
- Updated app now prefers `submodules/glTF-Sample-Assets/Models/**/*.glb` when present, with fallback to the legacy sample-model submodule.
- `origin/guc` predates current UI/SEO work. Its useful functional delta is small: add `.guc.usdz/.guc.png` paths, invoke a `guc` command, copy GUC outputs, and add one UI card.
- `origin/guc` is four commits on top of an older app state (`b9310b0`, `71c561a`, `d75fd7a`, `c8eda41`). The useful behavior has been ported into the new conversion-family pipeline rather than rebasing that stale UI branch directly.
- Existing generation flags live inside `collectFileInformation`; normal app operation expects generation flags off.
- Local Blender is `/Applications/Blender.app/Contents/MacOS/Blender`, version `5.1.2` per bundle metadata.
- OpenUSD tools resolve from `/Users/herbst/OpenUSD-26.05-native/bin`; `usdrecord`, `usdchecker`, `usdcat`, and `usdview` are available.
- Adobe glTF USD file-format plugin appears installed at `/Users/herbst/USD-Fileformat-plugins-2026.03-openusd-26.05/plugin/usd`.
- `guc` source was cloned to `/Users/herbst/git/guc` and built at `/Users/herbst/git/guc/build/bin/guc` against `/Users/herbst/OpenUSD-26.05-native`.
- npm reports `three@0.185.0` as the latest package and dist-tag `latest`; the existing app dependency is `@needle-tools/three@0.154.3`.
- The existing `submodules/three.js` remote is the Needle fork and only fetched official-looking tags up to `r183`; the official upstream route is needed for r185 provenance.
- Adjacent Khronos sample-model checkout exists at `../glTF-Sample-Models`, currently on `KHR_animation_pointer`, remote `origin=https://github.com/KhronosGroup/glTF-Sample-Models.git`, with `origin/main` at `d7a3cc8`.
- Adjacent Khronos sample-assets checkout exists at `/Users/herbst/git/glTF-Sample-Assets`.
- Added `submodules/glTF-Sample-Assets` with `--reference /Users/herbst/git/glTF-Sample-Assets`, pointing at upstream `2bac6f8c57bf471df0d2a1e8a8ec023c7801dddf`, which matches `origin/main` as of the fetch on 2026-06-27.
- Needle docs say public `USDZExporter.export(objectToExport)` returns a `Blob | null`; the runner now uses that API and explicitly sets `autoExportAnimations = true`. Reference: https://engine.needle.tools/docs/api/classes/Built-in_Components.USDZExporter.html
- Installed npm aliases/dependencies: `three-r185@npm:three@0.185.0`, `needle-engine-latest@npm:@needle-tools/engine@5.1.2`, and `playwright@1.61.1`.
- The one-command runner is `npm run conversions -- [--filter Box] [--limit 3] [--converters three-r185,blender-5-1,openusd-adobe-gltf,needle-engine,guc] [--skip-render] [--force] [--continue-on-error] [--dry-run]`. Up-to-date conversion and thumbnail outputs are skipped by default based on recorded provenance settings; `--force` intentionally reruns them.
- Representative Box matrix completed successfully for `guc`, `openusd-adobe-gltf`, `three-r185`, `needle-engine`, and `blender-5-1`. All five USDZ outputs passed `usdchecker`, and all five PNG thumbnails rendered with `src/lib/images/neutral.hdr`, 900x760, `usdrecord --renderer Storm` on macOS.
- `conversion-provenance.json` records source repo/commit, converter versions, GUC source commit, thumbnail settings, output paths, command provenance, and per-conversion status. The current manifest covers a full 118-asset `glTF-Binary` corpus run across five converter families.
- Full-corpus run results after the corrected Needle and three.js reruns: `three-r185` 118/118 success, `openusd-adobe-gltf` 118/118 success, `needle-engine` 118/118 success, `blender-5-1` 113/118 success, and `guc` 115/118 success.
- The app now uses provenance status when deciding whether a conversion is available. Failed conversions can leave diagnostic `.usdz` files on disk, but they are not advertised as downloadable/viewable conversions unless `conversion-provenance.json` marks them successful.
- Needle conversion now uses one headed Playwright Chromium session and one Vite exporter page backed by a real `<needle-engine>` element. Earlier test runs started too much browser/Vite work per asset; the current runner keeps the session open, reloads the exporter page only on retry, and closes it once after the full run.
- Needle's low success count was a runner integration issue, not a confirmed Needle Engine bug. The corrected exporter page gets the real Needle context from `engineElement.getContext()`, loads assets with `loadAsset(inputUrl, { context })`, attaches the loaded scene to `context.scene` with `GameObject.add`, registers imported clips through Needle's `Animation` component, attaches the real `USDZExporter` component, and destroys the scene after export. `Context.Current` is temporarily set only to that real runtime context because Needle component helpers consult it when they are called from the Playwright-triggered export function instead of from the normal render loop.
- Needle still reports feature/visual warnings for some valid conversions: morph target animation tracks are not exported, material color/opacity/texture-transform animation tracks are not exported, `KHR_node_visibility` and `KHR_materials_pbrSpecularGlossiness` are unknown to the current loader path, some vertex colors may be ignored by Quick Look, and several normal maps validate with non-standard scale warnings. These are semantic fidelity notes, not conversion failures.
- `NodePerformanceTest.glb.needle-engine.usdz` is about 109 MB / 112 MB on disk with 10,000 geometry references and validates slowly. One earlier full run saw `usdchecker` exit via `SIGSEGV`, but the package was readable by `usdcat`, had no missing package references in a follow-up scan, and subsequently passed `usdchecker` twice through the one-command runner.
- OpenUSD + Adobe glTF imports are loadable in `usdview` with the Adobe plugin even when textures are resolver-backed as `@0/Asset.glb[texture.png]@`. For validator-clean USDZ output, the converter now imports with `assetsPath`, packages the generated `.usdc` root layer first, includes extracted textures at package root, and transcodes extracted WebP textures to PNG while rewriting USD asset paths.
- The Adobe glTF plugin also writes generated anisotropy helper images into a repo-level `test/` directory for some assets. Those generated side-effect files were cleaned up after the full Adobe rerun.
- `usd/generate_thumbnail.py` used to leave `<asset>_Thumbnail.usda` wrapper stages beside USDZ inputs. Those generated wrappers were removed from the new sample-assets tree, and the helper now deletes them after rendering/linking. `NodePerformanceTest.glb.three-r185.usdz` can render a PNG and pass `usdchecker`, but OpenUSD may fail while opening/linking the huge wrapper stage; the conversion runner now treats that as a thumbnail-metadata warning if the PNG was freshly rendered.
- The original three.js r185 Node-side conversions were structurally valid but visually wrong: GLTFLoader silently returned null textures after Node image-loading failures, so USDZExporter emitted geometry-only packages. The r185 converter now uses a Playwright Chromium exporter page with a Vite alias that forces bare `three` imports to resolve to `three-r185`.
- three.js r185 was rerun across the full 118-asset corpus with `animations=true`, `animationFrameRate=60`, and `maxTextureSize=4096`; all 118 USDZ files pass `usdchecker` and have screenshots. A verification scan found texture files in 98 of the 100 image-bearing source GLBs. The two image-bearing r185 USDZs without texture files (`AnisotropyDiscTest`, `IridescenceSuzanne`) only reference their images through currently unsupported extension texture slots (`KHR_materials_anisotropy` / `KHR_materials_iridescence`), not through standard material maps.
- Browser-side three.js r185 warnings seen during valid conversions include unsupported double-sided material semantics, unsupported negative scale warnings, unknown `KHR_node_visibility` / `KHR_materials_pbrSpecularGlossiness`, missing normals for some meshes, and non-standard normal-map scale warnings from `usdchecker`.
- OpenUSD 26.05 `usdrecord --help` lists `Storm` and `GL`; it does not list `Metal`. The thumbnail helper now uses `Storm` on macOS so the canonical thumbnail render works with the installed OpenUSD.
- Blender 5.1.2 should not inherit the external native OpenUSD plugin path. Running Blender with `PXR_PLUGINPATH_NAME` and `DYLD_LIBRARY_PATH` unset avoids duplicate USD `Hio_OpenEXRImage` plugin-provider warnings while still producing valid USDZ.
- Disk space was freed during the full-corpus run; after the textured r185 rerun, about 24 GB were free on `/System/Volumes/Data`.
- `npm run build` passes with provenance-filtered static asset copying; after the corrected Needle and textured three.js r185 reruns, the build copied 1941 static items and wrote the site to `build/`.
- `npm run check` currently reports 100 errors and 1 warning in pre-existing type-debt areas: `src/USDZExporter.ts`, `src/hacks.js`, `src/routes/NeedleEngine.svelte`, and `src/routes/about/+page.svelte`. The homepage, new conversion runner/metadata route, real Needle exporter page, and browser three.js r185 exporter page do not contribute diagnostics in the latest check.
- The generated new conversion outputs are currently uncommitted files inside `submodules/glTF-Sample-Assets`. A durable snapshot will need a submodule-side commit or a decision to move generated outputs out of the upstream sample-assets checkout.
- USD Web Viewer links are now constructed client-side from the current page URL, so local/dev builds no longer hardcode `https://asset-explorer.needle.tools/` in the `file=` parameter.
- Conversion feature analysis is generated by `scripts/analyze_conversions.py` via `npm run analyze:conversions`. It uses OpenUSD 26.05 Python bindings (`pxr.Usd`, `UsdGeom`, `UsdShade`, `UsdLux`, `UsdSkel`, `Ar`, `Sdf`) for USDZ stage traversal, package metadata, and texture asset resolution. The npm script pins `PXR_PLUGINPATH_NAME` to `/Users/herbst/OpenUSD-26.05-native/plugin/usd` so the Adobe glTF plugin is not loaded during USDZ analysis.
- Analysis output is split into `conversion-analysis/<Asset>.json` plus `conversion-analysis/index.json`. The detail page attaches only the current asset's analysis; the index is the compact basis for a future converter-quality overview table.
- Current official-Python analysis output covers 118 assets and 582 successful conversions with no analyzer errors. Converter counts match provenance: `openusd-adobe-gltf` 118, `needle-engine` 118, `three-r185` 118, `guc` 115, `blender-5-1` 113.
- Texture byte totals for USDZ outputs come from USD-authored texture asset paths resolved through `Ar.OpenAsset`. Texture pixel dimensions are currently `null` for USD outputs because this OpenUSD Python build does not expose a Python `pxr.Hio` image-dimension API; the UI shows those cells as unknown rather than missing.
- `https://usd-assets.needle.tools/__data.json` is the hosted SvelteKit data payload for the USD-WG catalog and serializes the hierarchy produced by `/Users/herbst/git/usd-wg-assets/web/src/lib/files.ts`. `scripts/generate-usd-assets-manifest.mjs` now consumes that hosted data by default instead of scraping rendered HTML; `--local` runs the same `getFiles()`-compatible thumbnail/USD scan against the local checkout.
- The hosted USD-WG data currently publishes 217 catalog entries: 124 under `test_assets` and 93 under `full_assets`. The local `/Users/herbst/git/usd-wg-assets` checkout has 44 additional thumbnail-backed entries under untracked `full_assets/Vehicles/USD_Mini_Car_Kit_usdc/`, so the manifest records those as local-only diagnostics but does not publish them by default because the hosted URLs 404.
- The local `/Users/herbst/git/usd-wg-assets` checkout also has untracked `full_assets/Kitchen_set/`, but Kitchen Set has no matching `thumbnails/Kitchen_set.png`, so USD-WG's `getFiles()` discovery does not include it locally or in the hosted catalog. Kitchen Set is added explicitly to `usd-assets-manifest.json` with the requested Needle Cloud subheadline and a generic Needle Cloud link until the specific hosted asset URL/thumbnail is available.
- The asset-explorer homepage now reads `usd-assets-manifest.json`, links published USD-WG cards directly to the hosted USD asset URL from the hosted SvelteKit data, uses hosted thumbnails from the same payload, and groups the combined glTF/USD list into an expandable category tree. The category URLs keep source path segments for stability, while the sidebar displays human-readable labels.
- USD-WG homepage inclusion is currently disabled via `INCLUDE_USD_WG_ASSETS = false` in `src/routes/+page.server.ts`. The manifest generator and parsing code are left in place for later, but the app only returns glTF sample assets for now.
- With USD-WG disabled, the homepage hides the Collections sidebar when the category tree has only one root collection. glTF sample `metadata.json` tags are exposed as an `Asset groups` filter using the `group` query parameter.
- Homepage filters are multiplicative: repeated `group`/`tag` params include multiple required filters, while `notGroup`/`notTag` params exclude values. Clicking an active chip removes it; long-pressing or right-clicking a chip toggles exclusion. Facet counts show the currently matching count for enabling that facet versus the facet's total count.
- Conversion provenance and per-asset analysis JSON now live inside the active sample asset checkout, currently `submodules/glTF-Sample-Assets/conversion-provenance.json` and `submodules/glTF-Sample-Assets/conversion-analysis/`, beside the USDZ/PNG files they describe. The app and build copy step prefer those submodule-local files, with root-level legacy fallbacks only for older layouts.
- USD thumbnail rendering was over-lit because `usdrecord` added its default camera light on top of the neutral HDR dome light. `usd/generate_thumbnail.py` now disables the camera light when a dome light is provided, uses explicit sRGB color correction, high Storm complexity, vertical FOV 35, camera padding 1.12, dome intensity 1, and dome exposure 0. A temporary AlphaBlendModeTest matrix across Blender/OpenUSD/three/Needle/GUC showed consistent non-overexposed renders before changing the generated corpus.
- The full successful USDZ thumbnail set was rerendered sequentially with `nice -n 10 npm run render:thumbnails`: 582/582 thumbnails completed, with provenance updated in `submodules/glTF-Sample-Assets/conversion-provenance.json`. Counts by converter: GUC 115, Blender 113, OpenUSD Adobe glTF 118, Needle Engine 118, three.js r185 118.
- Source glTF reference thumbnails are generated by `npm run render:gltf-references` through a reused Playwright Chromium/Vite page at `/gltf-reference-renderer`. Files are written beside each source GLB as `<Asset>.glb.gltf-reference.webp` using WebP quality 0.9. The renderer uses three.js 0.185.0 with the same canonical render envelope as the USD thumbnails: neutral HDR environment, transparent background, sRGB output, no tone mapping, vertical FOV 35, camera padding 1.12, 900x760. The full 118-asset corpus completed with 118/118 success and is recorded in `sourceReferenceRenders` inside `submodules/glTF-Sample-Assets/conversion-provenance.json`.
- Follow-up visual checks found three render regressions after the first canonical rerender. `ABeautifulGame.glb.gltf-reference.webp` exposed bad transmissive ordering from sorting by object origins; the glTF reference renderer now computes world bounding boxes for transparent/transmissive meshes and uses a custom far-to-near transparent sort. `BarramundiFish.glb.needle-engine.webp` and `CarConcept.glb.blender-5-1.webp` exposed overly tight USD clipping; `usd/generate_thumbnail.py` now derives near/far from camera distance plus max asset extent.
- A controlled chrome-sphere diagnostic (`.render-diagnostics/chrome_sphere.glb`) converted through the Adobe/OpenUSD plugin to USDZ now uses corrected outward-facing sphere winding. Comparing that USDZ through the thumbnail pipeline against the source glTF in three.js shows the matching glTF reference setup is the same `src/lib/images/neutral.hdr`, no CPU mirroring, three.js/HDRLoader default `texture.flipY=true`, and `scene.environmentRotation.y = 270deg` (`Math.PI * 1.5`). USD thumbnails use the untransformed DomeLight texture.
- After applying the chrome-sphere alignment, `npm run render:gltf-references` rerendered the full source glTF reference set in one reused Playwright/Chromium session: 118/118 successes, no failed provenance entries. The updated provenance records `environmentTextureFlipY: true`, `environmentRotationY: 4.71238898038469`, no mirroring, FOV 35, and WebP quality 0.9 output.
- After confirming BoomBox matched between fresh Blender USDZ thumbnails and the source glTF reference, `nice -n 10 npm run render:thumbnails` rerendered the full successful USDZ thumbnail set again with no active X flip: 582/582 successes, no failed thumbnail provenance entries. Final converter thumbnail counts are GUC 115, Blender 113, OpenUSD Adobe glTF 118, Needle Engine 118, and three.js r185 118. Generated preview images were later converted from PNG to WebP q90 with alpha preserved; the generation scripts now render an intermediate PNG when needed, encode `cwebp -quiet -q 90 -alpha_q 100 -m 6`, and keep the final `.webp` artifact.
- USD thumbnail rendering is centralized in `scripts/lib/usd-thumbnail.mjs`. Both `scripts/render-thumbnails.mjs` and the one-command `scripts/convert-assets.mjs` call this shared helper for the canonical OpenUSD environment, camera/HDR settings, intermediate PNG render, WebP q90 encode, and provenance command recording. This removes the previous drift risk between conversion-time thumbnails and standalone thumbnail rerenders.
- `usd/generate_thumbnail.py` no longer writes shared `camera.usda` / `y_up.usda` temp files in the repo root. It creates a per-render temp directory, which prevents accidental collisions if thumbnail renders overlap.
- Needle Engine conversions now explicitly set `USDZExporter.quickLookCompatible = false` in the headed Playwright exporter page. The full 118-asset Needle corpus was regenerated with that setting and fresh WebP thumbnails; provenance verification found 118/118 Needle successes, 118/118 entries recording `quickLookCompatible:false`, and no missing USDZ or thumbnail files.
- A dry-run repeat of `npm run conversions -- --converters needle-engine --dry-run` skipped all 118 Needle outputs as current. This confirms that adding new assets should process only missing/new/stale outputs, while changes to converter settings, thumbnail settings, or `--force` trigger reruns.
- `NodePerformanceTest.glb` remains the slow Needle outlier: export, `usdchecker`, and Storm thumbnail rendering all complete, but the asset dominates runtime because the generated USDZ is about 99 MB and expensive to validate/render.
- `npm run build` passes after the quicklook-off Needle regeneration and WebP thumbnail updates. The build copied 1993 static items and wrote the static site to `build/`; Vite still reports the existing large-chunk warnings and a Needle/materialx browser externalization warning.
- Blender 5.1.2 previously authored a `/root/env_light` `DomeLight` because Blender's USD exporter defaults `convert_world_material=True`. The glTF conversion pipeline now passes `convert_world_material=False`, records `convertWorldMaterial:false` in Blender conversion provenance, and the Blender 5.1 USDZ/WebP/analysis set was regenerated with that setting.
- After disabling Blender world-material conversion, `AntiqueCamera` and every analyzed source asset with zero authored lights report zero Blender lights. Authored-light samples still preserve authored lights: `DirectionalLight` reports 1 source / 1 Blender light, `LightsRoundtrip` 5 / 5, and `LightsConstruct` 4 / 4.
- The full Blender 5.1 rerun still has five meaningful conversion failures: `AnimationPointerUVs` (Blender importer `KHR_animation_pointer` path), `CubeVisibility` and `LightVisibility` (`KHR_node_visibility` unsupported by Blender's importer), `IridescenceSuzanne` (`KHR_materials_iridescence` unsupported by Blender's importer), and `SheenWoodLeatherSofa` (`usdchecker` rejects Blender's packaged WebP texture asset extension).
